let PP = {};
PP.game = {};

/**
 * Create the game
 * @function PP.game.create
 * @param  config The object containin the game configuration. It must have the following
 *                mandatory attributes: canvas_width, canvas_height, canvas_background_color,
 *                canvas_id, scenes. 
 * @return A game object.
 */
PP.game.create = function (config) {
    console.log(config);
}